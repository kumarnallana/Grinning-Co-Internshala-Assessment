# Workspace Rules

## Git Commit Practice
- Always provide a ready-to-run, copy-pasteable `git commit` command block at the end of every response.
- Ensure the user can execute it manually in their terminal to commit recent changes.
